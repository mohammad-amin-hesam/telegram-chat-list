import React from "react";
import { StyledNoChat } from "../IndexStyles/IndexStyle";

const NoChat: React.FC = () => {
	return (
		<StyledNoChat>
			<h4>Select a chat to start messaging</h4>
		</StyledNoChat>
	);
};

export default NoChat;
